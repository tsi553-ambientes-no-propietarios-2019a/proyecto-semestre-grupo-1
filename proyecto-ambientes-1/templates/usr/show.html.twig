{% extends 'base.html.twig' %}

{% block title %}Usr{% endblock %}

{% block body %}
    <h1>Usr</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usr.id }}</td>
            </tr>
            <tr>
                <th>CI</th>
                <td>{{ usr.CI }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ usr.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('usr_index') }}">back to list</a>

    <a href="{{ path('usr_edit', {'id': usr.id}) }}">edit</a>

    {{ include('usr/_delete_form.html.twig') }}
{% endblock %}
