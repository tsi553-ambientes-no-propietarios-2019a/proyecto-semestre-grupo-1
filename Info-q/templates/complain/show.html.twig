{% extends 'base.html.twig' %}

{% block title %}Complain{% endblock %}

{% block body %}
    <h1>Complain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ complain.id }}</td>
            </tr>
            <tr>
                <th>Id_com</th>
                <td>{{ complain.idCom }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ complain.type }}</td>
            </tr>
            <tr>
                <th>Timestamp</th>
                <td>{{ complain.timestamp ? complain.timestamp|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ complain.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('complain_index') }}">back to list</a>

    <a href="{{ path('complain_edit', {'id': complain.id}) }}">edit</a>

    {{ include('complain/_delete_form.html.twig') }}
{% endblock %}
